const fs = require('fs'); let b = fs.readFileSync('src/pages/PruebasWalletApp.jsx', 'utf8'); b = b.replace(/const \[driverSearchTimeout, setDriverSearchTimeout\] = React.useState\(false\);/, 'const [driverSearchTimeout, setDriverSearchTimeout] = React.useState(false);\n  const [showEsperaPanel, setShowEsperaPanel] = React.useState(false);\n  const [enEsperaExtra, setEnEsperaExtra] = React.useState(false);'); b = b.replace(/if \(prev >= 60\) \{ \/\/ After 1 minute of UNACCEPTED search/, 'if (prev >= (enEsperaExtra ? 600 : 60)) { // After 1 or 10 mins of UNACCEPTED search'); fs.writeFileSync('src/pages/PruebasWalletApp.jsx', b);
